apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  # these are replica set labels
  labels:
    purpose: ds-demo
    project: roboshop
spec:
  replicas: 10
  selector:
    # these labels are used to select pods
    matchLabels:
      purpose: ds-demo
      project: roboshop
  # pod definition
  template:
    metadata:
      labels:
        purpose: ds-demo
        project: roboshop
    spec:
      containers:
      - name: nginx
        image: nginx